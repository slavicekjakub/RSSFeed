@model List<RSSFeed.Models.Article>

@{
    ViewData["Title"] = "Article list";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<center><h1>Tabulka článků v RSS feedu</h1></center>

<br />

@using (Html.BeginForm("ArticleList", "RssFeeds", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    <input type="hidden" value=@ViewBag.Id>
    <label for="from-date">From:</label>
    <input type="date" id="from-date" name="fromDate">
    <label for="to-date">To:</label>
    <input type="date" id="to-date" name="toDate">
    <br />
    <br />
    <button id="filter-button" class="btn btn-primary">Filter</button>
    <a asp-action="ArticleList" class="btn btn-success">Cancel filter</a>
    <a asp-action="ArticleList" class="btn btn-warning">Reload</a>
    <a asp-action="Index" class="btn btn-info">Back to List</a>
    
}

@if (@Model != null)
{
    if (@Model.Count > 0)
    {
        <table class="table">
            <thead>
                <tr>
                    <th>Title</th>
                    <th>Publish date</th>
                    <th>Author</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var @article in @Model)
                {
                    <tr>
                        <td>@article.Title</td>
                        <td>
                            @if (article.PublishDate != null)
                            {
                                @article.PublishDate
                            }
                        </td>
                        <td>@article.Author</td>
                    </tr>
                }
            </tbody>
        </table>
    }
}
<div>